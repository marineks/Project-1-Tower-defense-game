//Finalement il n'y aura qu'une instance de base donc juste une const

// class Base {
//     constructor(health) {
//         this.health = health;
//     }
// }



const base = {
    health : 200,
    attack: 0,
};


// Fetch element of lives (span) in the html

// link it to the base.health 